- stash(title => l('How to trade binaries?'));

%h1= stash->{title}
%p= l('Trading binaries is remarkably easy, and requires only 3 simple steps:')

.row
    .col
        %img.h10{:src => url_for('images/pages/home/step-1.svg')}
        %p= l('Define your position')
    .col
        %img.h10{:src => url_for('images/pages/home/step-2.svg')}
        %p= l('Get your price')
    .col
        %img.h10{:src => url_for('images/pages/home/step-3.svg')}
        %p= l('Make your trade')

%h2= l('Step 1: Define your position')
%p= l('First, you need to set the parameters of your trade. Here’s what you need to consider.')
%p.media
    %img.h10{:src => url_for('images/pages/get-started/define-your-position.svg')}

%h2= l('Choose a trade type')
%ul.bullet
    %li!= l('<strong>Rise/Fall</strong> - predict if the market will rise or fall from its current level.')
    %li!= l('<strong>Higher/Lower</strong> - predict if the market will end higher or lower than a price target.')
    %li!= l('<strong>Touch/No Touch</strong> - predict if the market will touch or not touch a price target.')
    %li!= l('<strong>In/Out</strong> - predict if the market will stay inside two price targets, or end between or outside them.')

%h2= l('Choose the underlying market')
%ul.bullet
    %li!= l('<strong>Currencies</strong> - trade all major FX pairs.')
    %li!= l('<strong>Stock Indices</strong> - trade all major worldwide stock indices.')
    %li!= l('<strong>Commodities</strong> - trade major commodities such as gold and oil.')
    %li!= l("<strong>Random Indices</strong> - trade [_1]'s propietary random indices.", stash->{website_name})

%h2= l('Choose the duration')
%p.media
    %img.h10{:src => url_for('images/pages/get-started/duration.svg')}
%p!= l('You can set the length of your trade, from 10 seconds to 365 days.')
%p!= l("But there's no need to wait until expiry. You can sell back long-term trades at any time to profit from changing market conditions.")

%h2= l('Choose the barrier(s)')
%p.media
    %img.h10{:src => url_for('images/pages/get-started/barriers.svg')}
%p= l("The barrier(s) you choose defines your position and triggers your payout.")
%p= l("You set the barrier(s) to define your position.")

%h2= l('Choose the payout')
%p.media
    %img.h10{:src => url_for('images/pages/get-started/payout.svg')}
%p= l('Each binary trade is for a pre-determined payout amount. You win the payout if your prediction is correct.')
%p= l('You can choose payouts from $1 to $100,000. This wide payout range is ideal for both small and large traders.')

%h2= l('Step 2: Get your trade price')
%p= l('[_1] prices are benchmarked against the interbank options market, so you always get best-in-class prices for your trades.', stash->{website_name})
%p= l('You get fair and transparent pricing, whatever your position.')

%h2= l('Step 3: Make your trade')
%ul.bullet
    %li= l('When you are satisfied with the trade price, instantly execute your trade.')
    %li= l("With [_1]'s unique platform, there's no risk of 'slippage' or gapping markets. And no hidden fees.", stash->{website_name})
    %li= l("You can sell back long-term trades at any time to profit from changing market conditions.")

%p.media
    %a.button.pjaxload{:href => url_for("/")}= l('Try it now')

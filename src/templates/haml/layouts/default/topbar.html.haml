#topbar
    = l("You're using a Virtual Account.")
    %a.button{:href =>url_for("/user/upgrade") }= l('Upgrade to a Real Account')
    %a.button{:href =>url_for("/user/upgrade/financial") }= l('Open a Financial Account')

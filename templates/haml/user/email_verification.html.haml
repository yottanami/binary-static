- stash(title => l('Email Verification'));

%h1= l('Email Verification')
%p= l("In order to proceed, please verify your email address.")
%p.center-aligned
    %a.button{:href => url_for("/user/resend_verification")}= l('Send Verification Email')
